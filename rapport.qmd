---
title: "Portfolio Tracker"
subtitle: "Rapport quotidien de suivi de portefeuille"
author: "Alban VIDELOUP"
date: today
date-format: "D MMMM YYYY"
lang: fr
format:
  pdf:
    # xelatex : gère nativement l'Unicode (€, accents, français) sans passer
    # par la machinerie lualatex/babel `luatexbase`/`selnolig` — celle-ci
    # dépend d'un paquet parfois indisponible à l'installation à la volée en CI.
    pdf-engine: xelatex
    documentclass: scrartcl
    classoption: ["DIV=11", "parskip=half"]
    papersize: a4
    fontsize: 11pt
    toc: true
    toc-depth: 2
    number-sections: true
    colorlinks: true
    fig-pos: "H"
    include-in-header:
      text: |
        \usepackage{microtype}
        \usepackage{booktabs}
        \setkomafont{disposition}{\rmfamily\bfseries}
        \addtokomafont{caption}{\small}
        \setcapindent{0pt}
execute:
  echo: false
  warning: false
  message: false
knitr:
  opts_chunk:
    dev: cairo_pdf
    fig.width: 6.3
    fig.height: 3.4
---

```{r}
#| label: setup

# ============================================================================
# PORTFOLIO TRACKER — rapport PDF quotidien
# Auteur : Alban VIDELOUP
#
# Ce document est le pendant IMPRIMABLE du dashboard HTML : mêmes données,
# mêmes calculs (le pipeline partagé R/pipeline.R), mais des graphiques
# statiques ggplot2 adaptés au papier et une mise en page LaTeX.
# Il est régénéré chaque jour de bourse par la GitHub Action et archivé,
# horodaté, dans le dossier exports/.
# ============================================================================

# Le pipeline complet — voir R/pipeline.R pour les explications détaillées.
source("R/pipeline.R", encoding = "UTF-8")

# ---- Habillage ggplot2 commun à tous les graphiques du rapport -------------
# Un thème sobre : pas de quadrillage secondaire, titres discrets, légende
# au-dessus du graphique (comme dans le dashboard).
theme_set(
  theme_minimal(base_size = 10) +
    theme(
      panel.grid.minor = element_blank(),
      plot.title.position = "plot",
      axis.title = element_text(color = "#52514e", size = 9),
      legend.position = "top",
      legend.justification = "left",
      legend.title = element_blank()
    )
)

# Étiquettes d'axes au format français (réutilisées partout).
lab_eur  <- scales::label_number(big.mark = " ", suffix = " €", accuracy = 1)
lab_pct  <- scales::label_percent(accuracy = 1, decimal.mark = ",", suffix = " %")
lab_pct1 <- scales::label_percent(accuracy = 0.1, decimal.mark = ",", suffix = " %")
```

> *`r info_donnees`.*
>
> *La version interactive de ce rapport (graphiques dynamiques, théorie
> illustrée) est consultable en ligne :
> [albanpjy.github.io/fin](https://albanpjy.github.io/fin/).*

# Synthèse

Le portefeuille (`r nrow(positions)` lignes :
`r positions |> count(type) |> mutate(l = paste0(n, " ", type)) |> pull(l) |> paste(collapse = ", ")`)
est valorisé **`r eur(total_valeur)`**, pour un capital investi de
`r eur(total_investi)`, soit une plus-value latente de
**`r eur(total_pnl)` (`r pct(total_pnl_pct)`)**. Depuis le 1^er^ janvier, la
performance s'établit à **`r pct(perf_ytd)`**.

`r if (nzchar(note_manuelles)) paste0("*Note — ", note_manuelles, "*")`

```{r}
#| label: table-synthese

# Les indicateurs calculés par le pipeline, mis en forme à la française.
# knitr::kable + booktabs : le standard des tableaux LaTeX soignés.
# format = "latex" + caption dans kable() : le tableau sort en flottant
# LaTeX insécable aux colonnes ajustées au contenu, au lieu du longtable
# (coupable entre deux pages, colonnes étroites) produit par la conversion
# markdown -> pandoc.
tibble(
  Indicateur = c(
    "Valeur du portefeuille", "Capital investi", "Plus/moins-value latente",
    "Performance depuis le 1er janvier", "Rendement annualisé",
    "Volatilité annualisée", "Ratio de Sharpe", "Ratio de Sortino",
    "VaR 95 % (1 jour)", "CVaR 95 % (1 jour)", "Drawdown maximal",
    "Bêta (vs CAC 40)"
  ),
  Valeur = c(
    eur(total_valeur), eur(total_investi),
    paste0(eur(total_pnl), " (", pct(total_pnl_pct), ")"),
    pct(perf_ytd), pct(ret_annualise), pct(vol_annuelle),
    num(sharpe), num(sortino),
    paste0(eur(var95_eur), " (", pct(var95_pct), ")"),
    pct(cvar95_pct), pct(dd_max), num(beta_pf)
  )
) |>
  knitr::kable(format = "latex", booktabs = TRUE, linesep = "", align = "lr",
               caption = "Indicateurs clés du portefeuille")
```

# Positions

```{r}
#| label: table-positions

# Chaque colonne est formatée en amont (chaînes de caractères) pour un
# contrôle total de la typographie : espaces des milliers, virgules…
# La plus/moins-value est scindée en deux colonnes (euros / pourcentage)
# pour éviter tout retour à la ligne dans les cellules.
positions |>
  arrange(desc(valeur)) |>
  transmute(
    Ligne = nom, Ticker = ticker, `Qté` = quantite,
    PMU = eur(pmu), Cours = eur(cours), Valeur = eur(valeur),
    `+/-` = eur(pnl), `+/- (%)` = pct(pnl_pct),
    Poids = pct(poids)
  ) |>
  knitr::kable(format = "latex", booktabs = TRUE, linesep = "", align = "llrrrrrrr",
               caption = "Détail des positions, triées par valeur décroissante")
```

```{r}
#| label: fig-allocation
#| fig-cap: "Répartition du portefeuille par ligne et par type d'actif"
#| fig-height: 2.9

# Barres horizontales triées : l'équivalent imprimable du treemap du
# dashboard. La couleur distingue actions et ETF (ordre fixe de la palette).
ggplot(positions, aes(valeur, reorder(nom, valeur), fill = type)) +
  geom_col(width = 0.72) +
  scale_fill_manual(values = c("Action" = col_s1, "ETF" = col_s2)) +
  scale_x_continuous(labels = lab_eur, expand = expansion(mult = c(0, 0.05))) +
  labs(x = "Valeur de marché", y = NULL)
```

# Performance

```{r}
#| label: fig-evolution
#| fig-cap: "Valeur quotidienne du portefeuille depuis sa constitution complète"

# La série calculée en section 5 du pipeline (report du dernier cours connu
# les jours sans cotation).
ggplot(valeur_quotidienne, aes(date, valeur)) +
  geom_line(color = col_s1, linewidth = 0.6) +
  scale_y_continuous(labels = lab_eur) +
  labs(x = NULL, y = "Valeur du portefeuille")
```

```{r}
#| label: fig-base100
#| fig-cap: "Comparaison en base 100 : portefeuille, CAC 40 et MSCI World (CW8)"

# Base 100 : toutes les séries partent de 100 (cf. pipeline, section 11) ;
# 112 en fin de courbe se lit « +12 % depuis le départ ».
ggplot(b100, aes(date, indice, color = serie)) +
  geom_line(linewidth = 0.6) +
  scale_color_manual(
    values = c("Portefeuille" = col_s1, "CAC 40" = col_s2,
               "MSCI World (CW8)" = col_s3)
  ) +
  labs(x = NULL, y = "Base 100")
```

```{r}
#| label: fig-mensuel
#| fig-cap: "Rendements mensuels du portefeuille (bleu : mois positifs, rouge : négatifs)"
#| fig-height: 2.7

ggplot(mensuel, aes(mois, r, fill = r >= 0)) +
  geom_col(width = 22, show.legend = FALSE) +
  scale_fill_manual(values = c("TRUE" = col_s1, "FALSE" = col_neg)) +
  scale_y_continuous(labels = lab_pct) +
  labs(x = NULL, y = "Rendement mensuel")
```

```{r}
#| label: fig-contrib
#| fig-cap: "Contribution de chaque ligne à la plus/moins-value totale"
#| fig-height: 2.9

ggplot(contrib, aes(pnl, factor(nom, levels = contrib$nom), fill = pnl >= 0)) +
  geom_col(width = 0.72, show.legend = FALSE) +
  scale_fill_manual(values = c("TRUE" = col_s1, "FALSE" = col_neg)) +
  scale_x_continuous(labels = lab_eur) +
  labs(x = "Plus/moins-value latente", y = NULL)
```

# Risque

```{r}
#| label: fig-drawdown
#| fig-cap: "Drawdown : baisse du portefeuille par rapport à son dernier sommet"
#| fig-height: 2.7

# La courbe vaut 0 quand le portefeuille est à son plus haut historique ;
# le point le plus bas est le drawdown maximal du tableau de synthèse.
ggplot(drawdowns, aes(date, drawdown)) +
  geom_area(fill = col_neg, alpha = 0.15) +
  geom_line(color = col_neg, linewidth = 0.6) +
  scale_y_continuous(labels = lab_pct) +
  labs(x = NULL, y = "Drawdown")
```

```{r}
#| label: fig-correlations
#| fig-cap: "Corrélations des rendements quotidiens entre les lignes"
#| fig-height: 4.6

# Échelle divergente : bleu = corrélation négative (diversifiant),
# gris = nulle, rouge = positive. Deux lignes très « rouges » entre elles
# font double emploi dans le portefeuille.
corr_long <- as_tibble(corr, rownames = "t1") |>
  pivot_longer(-t1, names_to = "t2", values_to = "rho") |>
  mutate(
    n1 = factor(positions$nom[match(t1, positions$ticker)], levels = labs_actifs),
    n2 = factor(positions$nom[match(t2, positions$ticker)], levels = labs_actifs)
  )

ggplot(corr_long, aes(n1, n2, fill = rho)) +
  geom_tile(color = "white", linewidth = 0.4) +
  geom_text(aes(label = format(round(rho, 2), decimal.mark = ",")), size = 2.3) +
  scale_fill_gradient2(
    low = "#104281", mid = "#f0efec", high = "#c0302f",
    midpoint = 0, limits = c(-1, 1), guide = "none"
  ) +
  coord_fixed() +
  theme(axis.text.x = element_text(angle = 40, hjust = 1)) +
  labs(x = NULL, y = NULL)
```

```{r}
#| label: table-medaf

# Bêta > 1 : la ligne amplifie le CAC 40 ; alpha > 0 : elle a fait mieux
# que ce que son niveau de risque laissait espérer (cf. dashboard, onglet
# Théorie, pour les formules et exemples).
medaf |>
  transmute(
    Ligne = nom, `Bêta` = num(beta),
    `Réalisé (ann.)` = pct(ret_realise),
    `Attendu (MEDAF)` = pct(ret_medaf),
    Alpha = pct(alpha)
  ) |>
  knitr::kable(format = "latex", booktabs = TRUE, linesep = "", align = "lrrrr",
               caption = "MEDAF appliqué ligne par ligne (marché : CAC 40, taux sans risque : 3 \\%)")
```

```{r}
#| label: fig-frontiere
#| fig-cap: "Frontière efficiente : 4 000 portefeuilles simulés (Monte Carlo). Étoile noire : allocation actuelle ; triangle vert : meilleur Sharpe simulé ; losanges : lignes individuelles."
#| fig-height: 4.2

# Le nuage des simulations, coloré par ratio de Sharpe (rampe bleue) ;
# l'enveloppe supérieure du nuage est la frontière efficiente.
sim    <- tibble(vol = vol_sim, ret = ret_sim, sharpe = sharpe_sim)
actifs <- tibble(vol = sqrt(diag(Sigma)), ret = mu, nom = labs_actifs)

ggplot(sim, aes(vol, ret)) +
  geom_point(aes(color = sharpe), size = 0.5, alpha = 0.5) +
  scale_color_gradient(low = "#cde2fb", high = "#104281", name = "Sharpe") +
  geom_point(data = actifs, aes(vol, ret), inherit.aes = FALSE,
             shape = 18, size = 2.4, color = "#52514e") +
  geom_text(data = actifs, aes(vol, ret, label = nom), inherit.aes = FALSE,
            size = 2.4, vjust = -1, color = col_muted) +
  annotate("point", x = vol_sim[i_tangent], y = ret_sim[i_tangent],
           shape = 17, size = 2.8, color = "#008300") +
  annotate("point", x = vol_pf_th, y = ret_pf_th,
           shape = 8, size = 3, stroke = 1, color = col_ink) +
  scale_x_continuous(labels = lab_pct) +
  scale_y_continuous(labels = lab_pct) +
  theme(legend.position = "right") +
  labs(x = "Volatilité annualisée", y = "Rendement annualisé (historique)")
```

# Couverture par options

## Les options sans jargon

Une **option** donne le *droit* — jamais l'obligation — d'acheter (**call**) ou
de vendre (**put**) un actif à un prix convenu (le **strike**), jusqu'à une date
donnée (l'**échéance**), contre un versement immédiat (la **prime**).

**L'analogie de l'assurance auto** — vous payez 500 € par an pour assurer une
voiture de 20 000 € : si rien n'arrive, la cotisation est « perdue » (tant
mieux) ; en cas de sinistre, l'assureur paie. Un put fonctionne à l'identique :
la prime est la cotisation, le strike est la valeur garantie, l'échéance est la
durée du contrat. Acheter un put à 95 % à 3 mois revient à assurer le
portefeuille contre toute baisse au-delà de −5 % pendant 3 mois.

**L'analogie de la promesse de vente** — un call ressemble à une option d'achat
immobilière : 5 000 € versés pour bloquer le droit d'acheter un bien 300 000 €
pendant 6 mois. Si le marché monte à 350 000 €, on exerce ; sinon on abandonne
les 5 000 €. Dans tous les cas, la perte maximale de l'acheteur d'option est
la prime — rien de plus.

**Ce qui fait le prix d'une option** : ① la *distance du strike* (garantir −5 %
coûte plus cher que garantir −15 %, comme une franchise basse) ; ② la *durée*
(le prix croît environ comme la racine carrée du temps) ; ③ la *volatilité* (le
conducteur nerveux paie plus cher — quand le marché s'affole, les primes de put
doublent ou triplent, si bien que la bonne assurance s'achète par temps calme).
Enfin, la **valeur temps** de l'option « fond » chaque jour, de plus en plus
vite près de l'échéance : c'est la rémunération du vendeur.

## Les trois stratégies appliquées au portefeuille

Le put protecteur crée un **plancher** sous le portefeuille moyennant une
prime ; le **collar** finance ce put par la vente d'un call, au prix d'un
**plafond** sur la hausse ; le **covered call** encaisse une prime en
échange de ce même plafond. Primes indicatives : put à 95 % ≈ 2,5 %,
call à 105 % ≈ 1,5 % (échéance ~3 mois).

```{r}
#| label: fig-put
#| fig-cap: "Put protecteur (strike 95 %) : un plancher contre la baisse"
#| fig-height: 2.9

# Profils calculés en section 10 du pipeline sur la valeur RÉELLE du
# portefeuille. Pointillés gris : sans couverture.
ggplot(payoffs, aes(sc)) +
  geom_line(aes(y = nu, linetype = "Sans couverture"), color = col_muted, linewidth = 0.6) +
  geom_line(aes(y = put_prot, linetype = "Put protecteur"), color = col_s1, linewidth = 0.7) +
  scale_linetype_manual(values = c("Sans couverture" = "dashed", "Put protecteur" = "solid")) +
  geom_vline(xintercept = 1, linetype = "dotted", color = "#c3c2b7") +
  scale_x_continuous(labels = lab_pct) +
  scale_y_continuous(labels = lab_eur) +
  labs(x = "Niveau du marché à l'échéance", y = "Valeur du portefeuille")
```

```{r}
#| label: fig-collar
#| fig-cap: "Covered call et collar : financer la protection"
#| fig-height: 2.9

pay_long <- payoffs |>
  select(sc, `Sans couverture` = nu, `Covered call (105 %)` = covered,
         `Collar (95 % / 105 %)` = collar) |>
  pivot_longer(-sc, names_to = "strategie", values_to = "valeur") |>
  mutate(strategie = factor(strategie, levels = c(
    "Sans couverture", "Covered call (105 %)", "Collar (95 % / 105 %)"
  )))

ggplot(pay_long, aes(sc, valeur, color = strategie, linetype = strategie)) +
  geom_line(linewidth = 0.65) +
  scale_color_manual(values = c(
    "Sans couverture" = col_muted, "Covered call (105 %)" = col_s2,
    "Collar (95 % / 105 %)" = col_s3
  )) +
  scale_linetype_manual(values = c(
    "Sans couverture" = "dashed", "Covered call (105 %)" = "solid",
    "Collar (95 % / 105 %)" = "solid"
  )) +
  geom_vline(xintercept = 1, linetype = "dotted", color = "#c3c2b7") +
  scale_x_continuous(labels = lab_pct) +
  scale_y_continuous(labels = lab_eur) +
  labs(x = "Niveau du marché à l'échéance", y = "Valeur du portefeuille")
```

**Dimensionnement d'une couverture indicielle** (options PXA sur CAC 40,
quotité de 10 € par point) — avec les chiffres du jour, portefeuille de
`r eur(total_valeur)` et bêta de `r num(beta_pf)`, indice à
`r format(round(cac_niveau), big.mark = " ")` points :

$$N_{puts} = \frac{V_{portefeuille} \times \beta}{\text{Indice} \times \text{quotité}}
= \frac{`r format(round(total_valeur), big.mark = " ")` \times `r num(beta_pf)`}{`r format(round(cac_niveau), big.mark = " ")` \times 10}
\approx `r num(n_puts_exact)` \text{ contrat(s)}$$

`r if (n_puts_exact < 1) paste0("Moins d'un contrat : pour un portefeuille de cette taille, la couverture indicielle est surdimensionnée (un put PXA couvre environ ", eur(cac_niveau * 10), " d'exposition). Alternatives : options sur actions individuelles (quotité de 100 titres), produits de bourse baissiers en petites coupures, ou augmentation temporaire de la poche de liquidités.") else paste0("Soit ", ceiling(n_puts_exact), " contrat(s) après arrondi. Coût indicatif de l'assurance (put 3 mois, strike 95 %) : environ ", eur(prime_put * total_valeur), ", soit ", pct(prime_put), " du portefeuille.")`

# Annexe — Fondements théoriques {#sec-theorie}

Cette annexe développe les modèles qui sous-tendent les indicateurs du rapport,
avec leurs hypothèses et leurs limites — plus en détail que l'onglet Théorie du
[dashboard](https://albanpjy.github.io/fin/).

## Le MEDAF (CAPM) : pourquoi seul le risque de marché est rémunéré

Le Modèle d'Évaluation Des Actifs Financiers (Sharpe, 1964 ; Lintner, 1965)
part d'un constat : le rendement d'un titre se décompose en une partie
expliquée par le marché et une partie qui lui est propre,
$$r_i = \alpha_i + \beta_i \, r_m + \varepsilon_i,$$
régression dont la pente est le **bêta** :
$$\beta_i = \frac{\mathrm{Cov}(r_i, r_m)}{\mathrm{Var}(r_m)}.$$

Le risque *spécifique* $\varepsilon_i$ (une grève, un échec commercial, un
scandale) disparaît dans un portefeuille diversifié : la loi des grands nombres
fait tendre la moyenne des $\varepsilon_i$ vers zéro. Un investisseur
diversifié ne subit donc que le risque *systématique* $\beta_i \, r_m$ — et un
marché à l'équilibre ne rémunère que le risque qu'on ne peut pas éliminer
gratuitement. D'où la relation centrale du MEDAF :
$$E(R_i) = R_f + \beta_i \left[ E(R_m) - R_f \right].$$

L'**alpha de Jensen** mesure l'écart entre le rendement réalisé et cette
espérance : $\alpha_i = \bar{R}_i - R_f - \beta_i(\bar{R}_m - R_f)$. Un alpha
durablement positif signale soit un talent de gestion, soit — plus souvent —
un facteur de risque que le modèle ne capture pas.

**Hypothèses du modèle** (toutes discutables) : investisseurs rationnels et
averses au risque, raisonnant en moyenne-variance sur une seule période ;
marchés sans frictions (ni impôts, ni coûts de transaction) ; prêt et emprunt
illimités au taux sans risque ; anticipations homogènes. **Limites
empiriques** : le bêta estimé est instable dans le temps (fenêtre et fréquence
d'estimation changent le résultat — ici, rendements quotidiens depuis la
constitution du portefeuille) ; la relation rendement/bêta observée est plus
plate que prévue (« low-beta anomaly ») ; des facteurs supplémentaires — taille,
valorisation, momentum — expliquent les rendements au-delà du marché, d'où les
modèles multi-facteurs de Fama-French (1993, 2015) qui étendent le MEDAF sans
le renier : le bêta de marché en reste le premier pilier.

## Mesurer le risque : volatilité, Sharpe, Sortino, VaR, drawdown

**Volatilité.** L'écart-type des rendements quotidiens, annualisé par
$\sigma_{an} = \sigma_j \sqrt{252}$ — la racine vient de l'additivité des
variances de rendements indépendants. Limite : elle traite symétriquement
hausses et baisses et sous-estime les queues épaisses des rendements réels
(les krachs sont plus fréquents que ne le prédit la loi normale).

**Ratio de Sharpe** $= (R_p - R_f)/\sigma_p$ : l'excès de rendement par unité
de risque total. C'est *l'échelle de comparaison universelle* entre placements
— mais il pénalise la volatilité haussière et perd son sens sur des
distributions très asymétriques (stratégies optionnelles).

**Ratio de Sortino** $= (R_p - R_f)/\sigma^-_p$ avec
$\sigma^- = \sqrt{\frac{1}{n}\sum \min(r_t - r_{cible}, 0)^2}\,\sqrt{252}$ :
même logique, mais seuls les écarts *sous* l'objectif comptent comme du
risque. Un Sortino très supérieur au Sharpe signale des pertes rares mais
concentrées.

**Value at Risk.** La VaR historique à 95 % sur 1 jour est le 5^e^ centile
empirique des rendements observés ; la version paramétrique suppose la
normalité : $\text{VaR}_{95\%} = -(\mu_j - 1{,}645\,\sigma_j)$. Trois
critiques classiques : elle ne dit *rien* de l'ampleur des pertes au-delà du
seuil ; elle n'est pas sous-additive (la VaR d'un portefeuille peut dépasser
la somme des VaR de ses composants) ; elle extrapole le passé. La **CVaR**
(Expected Shortfall), moyenne des pertes au-delà de la VaR, corrige les deux
premiers défauts — c'est elle que privilégient les régulateurs (Bâle III).

**Drawdown maximal.** $DD_t = V_t / \max_{s \le t} V_s - 1$, dont le minimum
est le drawdown maximal. Mesure « psychologique » par excellence : une perte
de 40 % exige un gain de 67 % pour être effacée, et la durée de recouvrement
compte autant que la profondeur.

## Markowitz : diversification et frontière efficiente

La variance d'un portefeuille de pondérations $w$ s'écrit
$\sigma_p^2 = w^\top \Sigma \, w = \sum_i \sum_j w_i w_j \sigma_{ij}$ : elle
dépend des **covariances** bien plus que des variances individuelles (dans un
portefeuille de $n$ titres équipondérés, il y a $n$ variances mais $n(n-1)$
covariances). Tant que les corrélations sont inférieures à 1, le risque du
portefeuille est inférieur à la moyenne pondérée des risques : c'est le seul
« repas gratuit » de la finance.

En balayant toutes les pondérations possibles, l'ensemble des couples
$(\sigma_p, E(R_p))$ atteignables dessine une région dont la bordure
supérieure gauche est la **frontière efficiente**. Deux portefeuilles y sont
remarquables : celui de **variance minimale globale**
($w \propto \Sigma^{-1}\mathbf{1}$) et le portefeuille **tangent**, qui
maximise le ratio de Sharpe ($w \propto \Sigma^{-1}(\mu - R_f\mathbf{1})$).
Combiné à l'actif sans risque, ce dernier engendre la **droite de marché des
capitaux** $E(R_p) = R_f + \frac{E(R_m)-R_f}{\sigma_m}\sigma_p$ : tout
investisseur rationnel du modèle détient le même portefeuille risqué, dosé
selon son aversion au risque — c'est le théorème de séparation de Tobin, et
le chaînon qui mène au MEDAF.

**Limite majeure** : $\mu$ et $\Sigma$ sont *estimés* sur un historique court
et bruité. L'optimiseur « maximise les erreurs d'estimation » (Michaud) : une
petite variation des données déplace beaucoup la frontière. La simulation
Monte Carlo du rapport (4 000 portefeuilles) visualise le nuage atteignable
sans prétendre à l'optimum exact ; en pratique, contraintes de poids,
rééquilibrages périodiques et humilité sur les espérances de rendement font
mieux que l'optimisation naïve.

## Les options : payoffs, parité et prix

À l'échéance $T$, les profils de gain sont
$$\text{Call} = \max(S_T - K, 0), \qquad \text{Put} = \max(K - S_T, 0).$$
Acheteur : perte limitée à la prime, gain potentiellement illimité (call) ou
égal au strike (put). Vendeur : gain limité à la prime, risque important —
d'où les appels de marge.

La **parité call-put**, $C - P = S_0 - K e^{-rT}$, relie les primes d'un call
et d'un put de mêmes strike et échéance : elle découle d'un simple argument
d'absence d'arbitrage (deux portefeuilles au payoff identique valent le même
prix aujourd'hui) et permet de vérifier la cohérence des prix cotés.

Le modèle de **Black-Scholes-Merton** (1973) donne le prix théorique d'un call
européen sur un actif sans dividende :
$$C = S_0 \, N(d_1) - K e^{-rT} N(d_2), \qquad
d_{1,2} = \frac{\ln(S_0/K) + (r \pm \sigma^2/2)\,T}{\sigma \sqrt{T}},$$
où $N$ est la fonction de répartition normale. Intuition : $N(d_2)$
s'interprète comme la probabilité (risque-neutre) de finir dans la monnaie.
Le seul paramètre inobservable est $\sigma$ ; en pratique, on *inverse* la
formule à partir des prix cotés pour obtenir la **volatilité implicite** — la
« météo » que le marché intègre dans les primes, celle-là même qui fait
tripler le prix des puts en période de stress. Les sensibilités du prix (les
**grecques** : delta $\partial C/\partial S$, thêta — l'érosion du temps —,
véga — la sensibilité à la volatilité) guident la gestion d'une couverture :
un put protecteur a un delta négatif qui amortit les baisses, et un thêta
négatif qui matérialise le coût de l'assurance jour après jour.

---

*Rapport généré automatiquement à partir des cours Yahoo Finance — document
pédagogique rédigé par Alban VIDELOUP, ne constitue pas un conseil en
investissement. Le dashboard interactif complet est consultable sur*
<https://albanpjy.github.io/fin/>.
